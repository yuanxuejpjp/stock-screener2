# 大科技AI股票筛选器

基于彼得·林奇风格优化的7步筛选法，专门针对大科技AI公司的股票监控和筛选应用。

## 🚀 在线体验

部署在 Streamlit Cloud: **https://your-app.streamlit.app**

## 功能特点

### 7步筛选逻辑
1. **Forward PEG ≤ 1.2** - 合理增长估值
2. **债务权益比 < 50%** - 健康财务结构
3. **TTM营收 ≥ 500亿美元** - 大市值公司
4. **Forward PE ≤ 25 且营收增长 ≥ 15%** - 合理估值与增长
5. **EPS增长率 20%-60%** - 稳健盈利增长
6. **自由现金流为正 且 FCF Yield ≥ 2.5%** - 现金流健康
7. **净现金/总资产 ≥ 20% 或 净现金 > 0** - 财务安全

### 加分项系统 (0-4分)
- Forward PE < 22 且 PEG < 0.9 → +1分 (估值优)
- AI营收占比 ≥ 30% 且增长 ≥ 30% → +1分 (AI业务强)
- ROE ≥ 25% → +1分 (高ROE)
- 机构持股 > 70% 且分析师推荐 ≤ 2.0 → +1分 (机构看好)

### 颜色编码
- 🟢 **绿色** - 通过全部7步 (强烈推荐)
- 🟡 **黄色** - 通过至少5步 (观察中)
- ⚪ **白色** - 通过少于5步 (不推荐)

### 宏观指标
- CNN恐惧贪婪指数实时监控
- S&P 500指数显示

## 安装与运行

### 1. 安装依赖
```bash
pip install -r requirements.txt
```

### 2. 运行应用
```bash
streamlit run app.py
```

应用将自动在浏览器中打开 (默认 http://localhost:8501)

## ☁️ 部署到 Streamlit Cloud

### 方法一：通过 GitHub 自动部署（推荐）

1. **准备代码**
   ```bash
   cd G:/Yuanxue2026/select_stock
   git init
   git add .
   git commit -m "Initial commit: AI Stock Screener"
   ```

2. **推送到 GitHub**
   ```bash
   # 在 GitHub 创建新仓库后
   git remote add origin https://github.com/你的用户名/仓库名.git
   git branch -M main
   git push -u origin main
   ```

3. **在 Streamlit Cloud 部署**
   - 访问 [https://streamlit.io/cloud](https://streamlit.io/cloud)
   - 点击 **"New app"**
   - 选择 GitHub 仓库并授权
   - 配置：
     - Main file path: `app.py`
     - Python version: `3.10` 或 `3.11`
   - 点击 **"Deploy"**

4. **完成！**
   - 应用地址：`https://你的应用名.streamlit.app`

### 方法二：直接上传

如果不想用 GitHub，可以在 Streamlit Cloud 直接上传代码文件。

### 管理应用

- **更新代码**: 推送到 GitHub 后自动重新部署
- **查看日志**: 在应用页面点击 "Logs"
- **删除应用**: Settings → Delete app

## 默认监控列表
- NVDA (NVIDIA)
- MSFT (Microsoft)
- GOOGL (Google/Alphabet)
- AMZN (Amazon)
- META (Meta Platforms)
- AAPL (Apple)

## 技术栈
- **Streamlit** - Web应用框架
- **yfinance** - Yahoo Finance数据API
- **Pandas** - 数据处理
- **Requests** - HTTP请求 (CNN API)

## 注意事项
- 股票数据来源于Yahoo Finance，可能存在延迟
- AI营收占比为预设估计值，非官方数据
- 投资有风险，筛选结果仅供参考，不构成投资建议
